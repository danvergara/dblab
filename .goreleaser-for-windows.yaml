before:
  hooks:
    - go mod tidy
builds:
  - id: windows-sqlite
    binary: dblab-sqlite
    env:
      - CGO_ENABLED=1
      - CC=x86_64-w64-mingw32-gcc
    goos:
      - windows
    goarch:
      - amd64
snapshot:
  name_template: "{{ .Tag }}-next"
